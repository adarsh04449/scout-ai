[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "python -m pip install -r api/requirements.txt",
  "python -m pip install -e market_research"
]

[start]
cmd = "python -m uvicorn api.app.main:app --host 0.0.0.0 --port $PORT"

